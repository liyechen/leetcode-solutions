package main

import "fmt"

func maximalRectangle(matrix [][]byte) int {
	if len(matrix) == 0 {
		return 0
	}

	// init zero [][]
	height := len(matrix)
	width := len(matrix[0])

	res := 0
	return res
}

func main() {
	fmt.Println("vim-go")
}
